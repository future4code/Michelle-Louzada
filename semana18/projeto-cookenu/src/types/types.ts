export type User = {
    id: string,
    email: string,
    password: string,
    name: string
  }

  export type UserInput = {
    id: string,
    email: string,
    cypherPassword: string,
    name: string
  }